Foo bar kk

$\sum_{i=1}^n 2^i$

```{python}
for i in range(10):
    print("ä " + str(i))
```

```{python}
import seaborn as sns
%matplotlib inline
from pylab import *
```

```{python}
import seaborn as sns
sns.boxplot(range(10))
figure()
plot(range(10))
```

```{python}

```
